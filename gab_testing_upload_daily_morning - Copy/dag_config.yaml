# ============================================================
#  DAG Config — gab_testing_upload_daily_morning
#  Every .sql file in this folder becomes one task in this DAG.
#  Tasks run in alphabetical order (use 01_, 02_ prefixes to control order).
# ============================================================

job_name    : test_gabriel_111          # DAG id shown in Airflow (no spaces)
label       : "Metabase"        # Friendly name shown in Airflow UI
schedule    : * 19 * * 1-4,6                   # Run every day at 7:00 AM  (HH:MM format)
description : "-"
owner       : gabriel
